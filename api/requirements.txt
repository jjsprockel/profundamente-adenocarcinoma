# Vercel's Python builder looks for requirements.txt next to the function
# entry point (api/index.py). Keep this in sync with backend/requirements.txt
# — uvicorn isn't actually used by Vercel's own ASGI invocation, but it's
# harmless to include and keeps the two files identical.
fastapi==0.115.12
uvicorn[standard]==0.34.2
python-multipart==0.0.20
reportlab==4.4.0
Pillow==11.2.1
pydicom==3.0.1
numpy==2.2.5
